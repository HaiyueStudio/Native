# Mining Boss supply crates and pickup audio — 2026-09-10

Level 8 (`asteroid-forge`) now drops sealed bomb supply crates during the entered, living `ore-reaper` Boss fight. The first check is at six seconds of active combat; subsequent checks occur at seeded 10–15 second intervals. At most one sealed crate is present; two already-floating bomb pickups suppress another crate. Crates drift down at 62 logical pixels/second, have 42 HP and a bounded sideways sway. Leaving the screen removes the crate. Pause freezes the timer and movement. Boss defeat, game over, home and level reset clear sealed crates.

Player projectiles participate in nearest swept collision with crates, rocks, enemies and bubbles; a nearer rock blocks a shot aimed at a crate. Purple laser and bomb blasts can also break a crate. Opening releases exactly one existing bomb pickup, without immediately crediting inventory. Touching the released pickup replenishes one bomb subject to the existing cap. Sealed crates do not damage or grant bombs on player contact. The field has its own seeded random stream, so supply scheduling does not perturb Boss attacks or asteroid throws.

The gold crate has a bomb emblem, three integrity lamps and hit flashes; it uses existing Engine GPU primitives and allocates no new sprite texture. Red, blue, purple weapon pickups and bomb pickups each have a distinct original synthesized chime. Collection triggers audio; uncollected color cycling does not. Total audio bank is now 18 cached mono WAVs (570,568 bytes / 1,139,552 native float PCM bytes), still using the existing 12-voice pool.

Verification completed: Games/native type checks; 8 focused crate/audio unit tests; 9 Native tests; real Chrome WebGPU normal and 280px narrow crate scenes, full audio lifecycle and prior mining collision regression. Assertions cover natural Boss-only supply timing, pause, repeated shots, one-drop semantics, player pickup, harmless sealed contact, laser destruction, nearer cover, Boss cleanup, all four actual collection sound hooks and no per-frame texture uploads. Screenshots were inspected after rendering actual frames.

Browser target build, iOS build, strict signature check, in-place device installation and launch passed. All 18 bundled WAVs match source hashes. Native playback probe and full-suite results are recorded separately when complete. Human pacing and pickup audibility remain gameplay acceptance checks.

Final verification: actual iPhone probe passed all 18 effects, including four pickup sounds with active native voices and no audio error; completion had zero voices and stopped engine. Host initialization includes crate state and 18 PCM buffers, with no host error. The existing saved 40% sound volume survived installation. Full Games suite: 513 tests, 491 passed, 20 skipped, 2 existing unrelated HYMUGEN golden-byte / viewer slot failures.
