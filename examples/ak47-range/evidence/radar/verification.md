# 自动装填、视野与雷达验证 — 2026-09-10

- 弹匣打空立即开始 2.2 秒装填；松开射击不取消装填，持续按住会在装填完成后继续射击。保留手动装填。
- 玩家视野为 120°，敌人视野为 60°；场景遮挡和战争迷雾沿用掩体检测。
- Engine GUI 在右上安全区域绘制半透明雷达。玩家位于中心，红点显示存活敌人，包括身后及掩体后的敌人；26 米以外的目标保留方位并收拢到边缘。方向与屏幕地面坐标一致，雷达不拦截触摸。

## 检查结果

- Games 与 Native 类型检查通过；AK47 18 项规则/资源测试、Native 8 项测试通过。
- Games 全部 25 款游戏完整构建通过。最终网页 bundle SHA-256 与浏览器验证记录一致。
- Chrome 原生 WebGPU：横屏 34 项、竖屏暂停/布局 3 项检查通过，无未分类浏览器或 GPU 错误。横屏截图已目视检查。
- iOS 构建和签名通过；包内 21 个模型/贴图资源逐字节验证通过。已更新 iPhone 15 Plus 上的 `org.haiyue.native.ak47range`。
- 真机 37 项交互检查通过，覆盖自动装填、分别设置的视野、雷达隐藏目标、实际 GUI 红点尺寸、安全区、重开清空、双摇杆和震动。原生截图已目视检查：雷达显示 3 个目标，而场景只渲染 1 个可见敌人。
- 验证运行日志呈现至 10200 帧，无 error/failure；关闭验证和截图开关后重新启动，普通模式日志呈现至 1680 帧，无 error/failure。手机保留普通游玩模式。

## 范围与证据

全仓 Games 测试为 517 项：495 通过、2 失败、20 跳过。现存两项 MUGEN 失败分别涉及 HYMUGEN 固定字节/哈希期望和 Viewer 虚拟列表断言；未修改这些模块，不宣称全仓测试通过。

`iphone-verification.json`、`iphone.png` 与 `iphone-host.jsonl` 保存真机检查、截图与日志；`iphone-normal-host.jsonl` 和 `normal-launch.json` 保存普通启动证据。`browser-*.json/png` 保存浏览器证据。`build-proof.json` 记录当前源码与网页/原生包摘要，其余日志保存构建、类型及测试输出。真机交互测试使用与 UITouch 相同的输入目标；不等同于多机型或人工手感验收。
