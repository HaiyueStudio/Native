# 六语言设置与齿轮修复验证

日期：2026-09-20。

## 更新内容

- 语言选择改用引擎 `GuiSelect`，支持中文、English、日本語、Français、Deutsch、Español。
- 补全新语言的游戏、日历、提示、胜利及内购界面文字；存档接受六种语言，兼容旧存档。
- 设置齿轮改为带留白的独立图标纹理，避免 Android 字体字形裁切。

## 真机结果

- Android X4000 / Android 14：更新安装成功，27/27 项交互检查通过。
- iPhone 15 Plus：签名构建、安装成功，27/27 项交互检查通过。
- 检查包含六语言菜单展开、边界、选择后关闭、关闭不改变选择，以及免费玩法和解锁界面。
- 人工核对截图：菜单六项全部可见且文字居中；法语、西班牙语重音字符正常；德语设置与解锁文案可完整显示；Android 齿轮完整。
- 诊断使用隔离的 `calendar-purchase-smoke` 存档命名空间。两台设备验证后均以正常方式重新启动。iPhone 正常启动日志包括 `engine-ready` 和 `present`，无错误事件。
- 没有触发结算。iOS 商品查询为 `unavailable`、Android 为 `offline`，均无价格且购买不可用。本次验证不能代替配置商品之后的真实商店沙盒交易测试。

## 自动检查

- Games 和 Native TypeScript 检查通过。
- Native 单元测试：32 项通过。
- 日历拼图新增的六语言存档及翻译完整性测试通过。
- 日历拼图 Web 目标构建、Android 与 iOS 原生构建通过。
- Games 全量测试：647 通过、20 跳过、2 失败、4 超时取消。失败/超时均位于未修改的 MUGEN 测试（HYMUGEN 固定哈希、viewer 虚拟列表、Petra 行为），详情见 `games-tests.log`。
- 全量构建首次在无关的 entanglement-path 遇到 60 秒超时，随后提高单项等待时限从该处续跑；续跑退出码为 0；两次构建日志共同覆盖全部 25 个游戏，均成功产出。

截图与逐项检查日志保存在本目录；开发者会员与沙盒配置见 [账号准备说明](../../docs/APPLE-DEVELOPER-SETUP.zh-CN.md)。
