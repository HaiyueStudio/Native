# 笔记与基础候选同步（2026-09-21）

用户截图对应 seed 3250120724、29/72 已填。R3C2 实际候选为 1、8、9，旧删除线为 1、4、6、8，未经筛选的删除线曾把无效的 4、6 重新画到盘面。

现在盘面与候选键共享基础规则过滤：1、8 的划线仍显示，4、6 不显示；高级技巧排除仍可显示与恢复。撤销/擦除后按新的盘面重新计算；关闭自动过滤时保持自由笔记模式。不修改原始删除记录，不影响后续解释的验证。

验证：207 项数独专项测试，11 项移动端测试，90 项安卓 X4000 诊断检查通过。定向类型检查、网页构建和 Android 构建通过；未修改引擎，未执行全仓检查。诊断使用独立存档。

实机正式棋局截图 `r3c2-fixed.png` 已核对：R3C2 只显示 1、8、9，1、8 保留删除线，4、6 已消失。升级前后题目、29 个已填数、笔记、删除记录、推理步数与辅助标记一致。
