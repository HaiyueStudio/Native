# 手动候选设置验证（2026-09-21）

- 新增“禁用基础规则自动剔除候选数”，默认关闭，支持中文、英文、日文并持久保存。
- 开启后数字键 1–9 全可用，笔记不再按基本或附加规则过滤，填写其他格不更新笔记。手动划除、恢复、撤销以及主动提示仍可使用。
- 保留原自动过滤偏好，手动模式关闭后恢复；开启期间自动过滤和自动候选展示控件置灰。
- 候选、设置、笔记同步专项测试 17 项通过，Native 测试 12 项通过；两端类型检查和构建通过，未执行全仓检查。
- 安卓 X4000 覆盖安装成功，独立测试模式 113 项检查通过，详见 host.jsonl。
- settings.png 与 manual-enabled.png 已视觉检查：标题、说明和禁用状态完整可读。
- 已退出独立测试模式并恢复正常游戏。网页的设置面板也支持同一手动行为。

安装前后存档比较：棋局、填写、笔记、划除记录、提示进度、答案和设置一致（计时不参与比较）。
