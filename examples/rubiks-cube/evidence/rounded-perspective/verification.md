# 圆角、透视与间隙减半验证 — 2026-09-10

- 使用 Engine 公共 `createRoundedBox3D` 替换本体与面片几何。镜面块按实际尺寸生成圆角，共享尺寸复用几何。
- 本体间隙由 0.055 减为 0.0275，面片间隙由 0.12 减为 0.06；面片沿法线外移，保持与本体的原有贴合关系。
- 使用 45° 透视相机，横竖屏自动调整观察距离及中心位置，保留 Engine GUI 布局。
- Games / Native 类型检查、游戏目录 ESLint、24 项规则与存档测试通过，目标浏览器构建通过。
- Chrome 原生 WebGPU：390×844 与 844×390 下各验证四种魔方的打乱、GUI 转层、撤销、历史还原、表面拖动与精确回退；确认透视投影和有界几何缓存。浏览器/GPU 错误为 0。两张截图均已目视检查。
- Xcode Debug iPhone 构建成功，已更新安装并启动。真机日志到第 1080 帧，确认透视相机，安全区内逻辑尺寸 430×839，渲染尺寸 860×1678。没有 error/capture-error。第 120 帧截图显示圆角四阶首页与缩小后的间隙，已目视检查。
- 本次未进行真机横屏及所有类型的触摸验收；对应自动交互验证来自浏览器。全仓测试/构建的既有问题见上一级 verification.md，此次仅报告上述定向检查通过。

构建与源码哈希见 build-proof.json；浏览器 JSON 包含实际被加载的 bundle 哈希。iphone.png 为 App 自身渲染帧，不含系统状态栏。
