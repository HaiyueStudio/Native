# Absolute server-local paths. Never place credentials/private keys in the app.
GOOGLE_APPLICATION_CREDENTIALS=/run/secrets/google-play-service-account.json
ENTITLEMENT_PRIVATE_KEY_FILE=/run/secrets/calendar-entitlement-private.pem
HOST=127.0.0.1
PORT=8080
